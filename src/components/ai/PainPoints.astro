---
import { aiTranslations } from '../../i18n/ai';

interface Props {
    lang: 'pt' | 'en' | 'es';
}

const { lang } = Astro.props;
const t = (key: keyof typeof aiTranslations['pt']) => aiTranslations[lang][key];

const cards = [
    {
        icon: 'ph-battery-warning',
        color: 'text-red-600 bg-red-100',
        title: t('pain.1.title'),
        desc: t('pain.1.desc')
    },
    {
        icon: 'ph-fire',
        color: 'text-orange-600 bg-orange-100',
        title: t('pain.2.title'),
        desc: t('pain.2.desc')
    },
    {
        icon: 'ph-question',
        color: 'text-blue-600 bg-blue-100',
        title: t('pain.3.title'),
        desc: t('pain.3.desc')
    },
    {
        icon: 'ph-heart-break',
        color: 'text-purple-600 bg-purple-100',
        title: t('pain.4.title'),
        desc: t('pain.4.desc')
    }
];
---

<section class="bg-brand-light py-20">
    <div class="container mx-auto px-6">
        <div class="text-center max-w-3xl mx-auto mb-16">
            <h2 class="text-3xl lg:text-4xl font-playfair font-bold text-gray-900 mb-4">
                {t('pain.title')}
            </h2>
            <p class="text-gray-600 text-lg">{t('pain.subtitle')}</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
            {cards.map(card => (
                <div class="bg-white p-8 rounded-lg flex gap-5 items-start">
                    <div class="flex-shrink-0 text-4xl text-brand-green mt-1">
                        <i class={`ph ${card.icon}`}></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-xl mb-2">{card.title}</h3>
                        <p class="text-gray-500 text-sm leading-relaxed">{card.desc}</p>
                    </div>
                </div>
            ))}
        </div>
    </div>
</section>
