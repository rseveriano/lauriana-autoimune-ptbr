# Plan: Improve Portfolio & Campaign Sites

## Goal
Raise the quality of both the main portfolio site (`/`, `/pt/`, `/en/`, `/es/`) and the AI autoimmune campaign landing (`/ai`) by addressing known absolute bans, DESIGN.md deviations, and UX weaknesses.

## Decisions Confirmed
- **AI landing is the business priority** — fixed first.
- **Start with critique, then fix** — data-driven, not guesswork.
- **Main site fonts can change** — Inter + Outfit flagged as overused; open to a distinctive replacement.

## Current State (from detector + code inspection)

### AI Landing — Priority Surface
- **Absolute ban hit:** Side-stripe border (`border-l-4`) in `src/components/ai/Hero.astro` line 46.
- **DESIGN.md deviation:** Generic `bg-gray-50` used in price section instead of Mist.
- **DESIGN.md deviation:** Header CTA uses `rounded-full` (pill) instead of 8px radius per AI landing spec.
- **DESIGN.md deviation:** Hardcoded `hover:bg-yellow-700` on CTA instead of darker Coral Clay.
- **Typography issue:** Detector flags Lato as overused single-font on AILayout (Playfair is loaded but not properly detected as display).
- **Conversion risk:** The offer section glass cards use inconsistent border radii and hover states.

### Main Site (Portfolio) — Secondary
- **Absolute ban hit:** Identical icon + heading + text card grid in `Services.astro`.
- **Typography issue:** Detector flags Inter as overused.
- **Layout issue:** Header uses `backdrop-blur-md` on nav — glassmorphism used as default.
- **Missing:** No scored UX review has ever been run on either surface.

## Execution Phases

### Phase 1: Critique AI Landing
Run a scored UX review of the AI landing to establish a baseline and catch issues the detector missed.
- Command: `/impeccable critique src/components/ai/`
- Deliverable: Heuristic score + prioritized issue list (p0/p1/p2).

### Phase 2: Fix AI Landing
Address all p0/p1 issues from the critique plus the known absolute bans.
- Command: `/impeccable polish src/components/ai/`
- Known targets: Hero (remove side-stripe), Offer (fix radii & gray backgrounds), Price (fix hardcoded colors & CTA shape), Header (fix CTA radius).

### Phase 3: Critique Main Site
Run a scored UX review of the portfolio to inform Phase 4.
- Command: `/impeccable critique src/components/LandingPage.astro`
- Deliverable: Heuristic score + prioritized issue list.

### Phase 4: Fix Main Site Layout & Cards
Address the identical card grid, spacing rhythm, and glassmorphism in nav.
- Command: `/impeccable layout src/components/` (focused on Services, About, Hero)

### Phase 5: Main Site Font Refresh
Replace the overused Inter + Outfit pair with a distinctive, brand-appropriate pairing.
- Command: `/impeccable typeset src/components/`
- Constraint: Keep the two-font max per DESIGN.md. Pick a pairing that honors the "Grounded, Human, Precise" brand personality.

### Phase 6 (Optional): Motion & Delight
If time/budget allows after the above are solid:
- `/impeccable animate src/components/LandingPage.astro` — purposeful entrance motion for portfolio sections.
- `/impeccable colorize src/components/ai/` — deepen the campaign palette if the critique revealed flatness.

## Decisions Locked In
- **Main site font direction:** Warmer and more human. Humanist sans for body + distinctive serif or rounded display for headings. Grounded and approachable.
- **AI landing polish scope:** Visual + copy. If the critique flags unclear or buzzword-heavy copy, polish rewrites `src/i18n/ai.ts` strings in all three languages (pt/en/es).

## Next Step
Begin Phase 1: `/impeccable critique src/components/ai/`
