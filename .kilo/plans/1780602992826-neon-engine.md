# Commit Plan

## Changes to commit (16 modified files)

### Font swap — main site
- `package.json` / `package-lock.json`: added `@fontsource/merriweather` and `@fontsource/merriweather-sans`
- `tailwind.config.mjs`: `font-sans` → Merriweather Sans, `font-heading` → Merriweather
- `src/layouts/Layout.astro`: imports and global styles updated
- `AGENTS.md`: documentation updated to reflect new fonts

### UI refinements — both sites
- **Headers**: removed `backdrop-blur` (both main and AI)
- **Border radius normalization**: AI components `rounded-2xl/3xl/4xl` → `rounded-lg`; main site About/Header `rounded-3xl` → `rounded-4xl`
- **Section labels**: removed `uppercase` class from several labels
- **AI CTA buttons**: `bg-brand-green` → `bg-brand-accent`, hover → `#C0563E`
- **AI PainPoints**: 4-column grid → 2-column horizontal card layout, `bg-gray-50` → `bg-brand-light`
- **AI Offer**: `bg-gray-50` → `bg-brand-light`
- **Services card**: class reordering, responsive icon margin
- **AI Bio**: removed decorative border-l from CRN section
- **AI Hero**: decoration border changed from `border-l-4 border-brand-green` → `border border-gray-100`

## Untracked files (TO be committed)
- `.kilo/` — Kilo tool config
- `.impeccable/` — Impeccable skill working files
- `DESIGN.md`, `PRODUCT.md` — design documentation

## Commit message
```
style: swap main site fonts to Merriweather and refine UI across both sites

Replace Inter/Outfit with Merriweather/Merriweather Sans for the main site.
Normalize border radii, remove backdrop-blur from headers, adjust CTA colors
to brand-accent, restructure pain points to 2-column layout, and switch
section backgrounds from gray-50 to brand-light.
```

## Steps
1. `git add` all 16 modified files
2. `git commit` with the message above
3. `git status` to verify
